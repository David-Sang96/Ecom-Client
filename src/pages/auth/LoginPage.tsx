import { LoginForm } from "@/components/auth/LoginForm";

const LoginPage = () => {
  return (
    <section className="p-2 pt-20">
      <LoginForm />
    </section>
  );
};

export default LoginPage;
